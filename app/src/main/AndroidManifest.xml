<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.ButtonDescribe"
        tools:targetApi="31">

        <activity
            android:name="com.example.presentacion.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Corrected declaration for the com.example.presentacion.Juego activity -->
        <activity
            android:name="com.example.presentacion.Juego"
            android:exported="true">
            <!-- Add intent filters if needed -->
        </activity>

        <activity
            android:name="com.example.presentacion.Musica"
            android:exported="true">
            <!-- Add intent filters if needed -->
        </activity>
        <activity
            android:name="com.example.presentacion.LLamada"
            android:exported="true">
            <!-- Add intent filters if needed -->
        </activity>

    </application>

</manifest>
